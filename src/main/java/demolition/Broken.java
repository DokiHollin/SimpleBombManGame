package demolition;

import processing.core.PImage;
/**
 * Broken wall type
 */
public class Broken extends Types{
    /**
     * constructor for Broken which extends Types
     * @param x,x-axis
     * @param y,y-axis
     * @param sprite,PImage for the broken wall
     */
    public Broken(int x, int y, PImage sprite) {
        super(x, y, sprite);
    }
    
}
